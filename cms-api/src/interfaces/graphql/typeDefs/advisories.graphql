type Advisory {
  id: ID
  advisoryId: Int
  userId: Int
  categoryId: ID
  category: String
  title: String
  content: String
  source: String
  locationAddress: String
  locationDetails: AdvisoryLocation
  tags: [Tag]
  verified: Boolean
  draft: Boolean
  attachments: [Urls]
  publishedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

type AdvisoryPrimary {
  id: ID
}

type Query {
  getAdvisories(where: AdvisoryFilter): [Advisory]!
  getAdvisory(where: AdvisoryPrimaryInput!): Advisory!
}

type Mutation {
  createAdvisoryDraft: AdvisoryPrimary
  saveAdvisory(where: AdvisoryPrimaryInput, data: AdvisoryInput!): Advisory
  publishAdvisory(where: AdvisoryPrimaryInput, data: AdvisoryInput!): Advisory
  deleteAdvisory(where: AdvisoryPrimaryInput!): Boolean
  attachmentUrlAdvisory(files: [AttachmentInput]): [Urls]
}

type Urls {
  fileName: String
  downloadUrl: String
  uploadUrl: String
}

input AdvisoryInput {
  advisoryId: Int
  userId: Int
  categoryId: ID
  category: String
  title: String
  content: String
  source: String
  placeId: String
  tags: [TagInput]
  verified: Boolean
  draft: Boolean
  attachments: [AttachmentInput]
  publishedAt: DateTime
}


input AdvisoryPrimaryInput {
  id: ID!
}

input AdvisoryFilter {
  verified: Boolean
  draft: Boolean
}

input AttachmentInput {
  fileName: String
}

type AdvisoryLocation {
  placeId: String
  lng: String
  lat: String
  country: String
  region: String
  province: String
  city: String
  district: String
  barangay: String
  street: String
  streetNumber: String
  locationLevel: String
}